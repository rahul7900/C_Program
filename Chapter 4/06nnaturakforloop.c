#include <stdio.h>

int main()
{
    int n;
    printf("Enter the value of n\n");
    scanf("%d", &n);
    for ("%d"; n >0 ; n--)
    {
        printf("%d is the value of n\n", n - 1);
    }
    return 0;
}