#include <stdio.h>

int main()
{
    int a;
    printf("Enter the value of a\n");
    scanf("%d", &a);
    for ( "%d" ; a < 10; a++)
    {
        printf("Your value is %d\n", a);
    }
    return 0;
}